function loginAs(role) {
  alert("Redirecting to " + role + " login/signup page...");
  // Here you can redirect to actual login pages
  // window.location.href = role.toLowerCase() + "-login.html";
}

